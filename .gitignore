.env
node_modules/
dist/
*.log
.DS_Store

# project-local flightrec store (sessions, screenshots, memory — never commit)
.flightrec/

# local smoke-test store (never commit)
.test-flightrec/
.test-flightrec-app/

# session data never belongs in the repo (legacy / accidental paths)
sessions/
raw.jsonl
digest.jsonl

# desktop Electron build output
desktop/node_modules/
desktop/out/
desktop/dist/
desktop/release/
desktop/resources/flightrec-runtime/
